using System;

namespace Stampsy.Social
{
    public enum LoginProgress
    {
        Authorizing,
        PresentingAccountChoice,
        PresentingAuthController,
        PresentingSafari
    }
}

